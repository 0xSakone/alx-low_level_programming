#include "main.h"

/**
 * _abs - check if a given is result
 * The argument should be int
 *
 * @n: n value to check to check
 *
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);

}
