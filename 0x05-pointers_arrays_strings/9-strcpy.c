#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strcpy - print last n of half
 * @s1: given string
 * @s2: given string 2
 * Return: copied
 */
char *_strcpy(char *s1, char *s2)
{
	return (strcpy(s1, s2));
}
